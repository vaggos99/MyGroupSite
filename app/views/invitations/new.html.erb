<%= form_with scope: :invitation, url: invitations_path,
local: true do |form| =%>

<div class="field">
  <%=form.label :friend_id =%>
<%= form.select :friend_id,User.where.not(id: Invitation.where(user_id:current_user.id).select(:friend_id)).where.not(id:current_user.id).map{|c| [c.name,c.id]}, class:"form-select" %>
</div>


  <%= form.number_field :user_id,value: current_user.id,type: :hidden%>
  <div class="field">
    <%= form.submit "Add friend", class: 'btn btn-success' %>
  </div>
<% end %>
